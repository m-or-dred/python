string, width = input(), int(input())

if len(string) % width:  output = [''] * (len(string) // width + 1)

else:  output = [''] * (len(string) // width)

for i in range(len(output)):  output[i] = string[width * i : width * (i + 1)]

for line in output:  print(''.join(line))
