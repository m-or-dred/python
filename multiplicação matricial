B = [[ 7,  8,  9],
	 [10, 11, 12]]

A = [[ 1,  4 ],
	 [ 2,  5 ],
	 [ 3,  6 ]]

M = [[0 for m in range(len(A))] for p in range(len(B[0]))]

for m in range(len(A)):

	for n in range(len(B)):

		for p in range(len(B[0])):

			M[m][p] += A[m][n] * B[n][p]

print(M)
